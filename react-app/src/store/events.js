const initialState = {

};

const eventsReducer = (state = initalState, action) => {
  let newState;
  switch (action.type) {
    default:
        return state;
  }
};


export default eventsReducer;
