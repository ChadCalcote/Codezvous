const initialState = {

};

const sessionReducer = (state = initalState, action) => {
  let newState;
  switch (action.type) {
    default:
        return state;
  }
};



export default sessionReducer;
