const initialState = {

};

const groupsReducer = (state = initalState, action) => {
  let newState;
  switch (action.type) {
    default:
        return state;
  }
};


export default groupsReducer;
